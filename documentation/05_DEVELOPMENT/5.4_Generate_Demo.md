# 5.4 Generate Demo — สร้าง Demo เว็บ

## วัตถุประสงค์

พัฒนา Frontend Demo ด้วย React + Tailwind CSS โดยใช้ Mock Data แสดงผลในตารางและ components ต่างๆ เพื่อทดสอบ UI/UX ก่อนเชื่อมต่อ Backend จริง

---

## ขั้นตอนการทำ

### 1. สร้าง Mock Data

#### 1.1 สร้างโครงสร้างไฟล์ Mock Data

```bash
cd frontend/src
mkdir -p data/mock
```

#### 1.2 สร้างไฟล์ Mock Data

**ไฟล์ `src/data/mock/products.ts`:**

```typescript
import { Product } from '@/types/models';

export const mockProducts: Product[] = [
  {
    id: 1,
    name: 'iPhone 15 Pro Max',
    slug: 'iphone-15-pro-max',
    description: 'สมาร์ทโฟนรุ่นท็อปจาก Apple',
    price: 45900,
    originalPrice: 49900,
    stock: 50,
    categoryId: 1,
    categoryName: 'Smartphones',
    imageUrl: 'https://via.placeholder.com/300x300?text=iPhone+15',
    images: [
      'https://via.placeholder.com/600x600?text=iPhone+15+1',
      'https://via.placeholder.com/600x600?text=iPhone+15+2',
    ],
    rating: 4.8,
    reviewCount: 245,
    isActive: true,
    createdAt: '2025-01-10T10:00:00Z',
  },
  {
    id: 2,
    name: 'Samsung Galaxy S24 Ultra',
    slug: 'samsung-galaxy-s24-ultra',
    description: 'สมาร์ทโฟนเรือธงจาก Samsung',
    price: 42900,
    stock: 35,
    categoryId: 1,
    categoryName: 'Smartphones',
    imageUrl: 'https://via.placeholder.com/300x300?text=Galaxy+S24',
    rating: 4.7,
    reviewCount: 189,
    isActive: true,
    createdAt: '2025-01-11T10:00:00Z',
  },
  // เพิ่ม products อื่นๆ...
];
```

**ไฟล์ `src/data/mock/categories.ts`:**

```typescript
import { Category } from '@/types/models';

export const mockCategories: Category[] = [
  { id: 1, name: 'Smartphones', slug: 'smartphones', icon: 'fa-mobile-alt', productCount: 45 },
  { id: 2, name: 'Laptops', slug: 'laptops', icon: 'fa-laptop', productCount: 32 },
  { id: 3, name: 'Accessories', slug: 'accessories', icon: 'fa-plug', productCount: 128 },
  { id: 4, name: 'Fashion', slug: 'fashion', icon: 'fa-tshirt', productCount: 256 },
  { id: 5, name: 'Books', slug: 'books', icon: 'fa-book', productCount: 512 },
  { id: 6, name: 'Home & Living', slug: 'home-living', icon: 'fa-home', productCount: 89 },
];
```

**ไฟล์ `src/data/mock/users.ts`:**

```typescript
import { User } from '@/types/models';

export const mockUsers: User[] = [
  {
    id: 1,
    email: 'admin@example.com',
    firstName: 'Admin',
    lastName: 'System',
    role: 'admin',
    avatar: 'https://via.placeholder.com/150?text=Admin',
    createdAt: '2025-01-01T00:00:00Z',
  },
  {
    id: 2,
    email: 'john@example.com',
    firstName: 'John',
    lastName: 'Doe',
    role: 'customer',
    avatar: 'https://via.placeholder.com/150?text=John',
    createdAt: '2025-01-05T10:30:00Z',
  },
];
```

**ไฟล์ `src/data/mock/orders.ts`:**

```typescript
import { Order } from '@/types/models';

export const mockOrders: Order[] = [
  {
    id: 1001,
    orderNumber: 'ORD-2025-001001',
    userId: 2,
    status: 'delivered',
    paymentStatus: 'paid',
    subtotal: 45900,
    tax: 3213,
    shippingFee: 50,
    discount: 500,
    total: 48663,
    shippingAddress: '123 ถนนสุขุมวิท กรุงเทพฯ 10110',
    items: [
      {
        id: 1,
        productId: 1,
        productName: 'iPhone 15 Pro Max',
        productImage: 'https://via.placeholder.com/100?text=iPhone',
        quantity: 1,
        price: 45900,
        subtotal: 45900,
      },
    ],
    createdAt: '2025-01-15T14:30:00Z',
  },
];
```

---

### 2. สร้าง Common Components

#### 2.1 สร้าง Button Component

**ไฟล์ `src/components/common/Button.tsx`:**

```typescript
import React from 'react';
import clsx from 'clsx';

interface ButtonProps {
  variant?: 'primary' | 'secondary' | 'danger' | 'outline';
  size?: 'sm' | 'md' | 'lg';
  children: React.ReactNode;
  onClick?: () => void;
  disabled?: boolean;
  type?: 'button' | 'submit' | 'reset';
  className?: string;
}

export function Button({
  variant = 'primary',
  size = 'md',
  children,
  onClick,
  disabled = false,
  type = 'button',
  className,
}: ButtonProps) {
  const baseClasses = 'rounded-lg font-medium transition-colors inline-flex items-center justify-center';

  const variantClasses = {
    primary: 'bg-blue-600 text-white hover:bg-blue-700',
    secondary: 'bg-gray-200 text-gray-800 hover:bg-gray-300',
    danger: 'bg-red-600 text-white hover:bg-red-700',
    outline: 'border-2 border-blue-600 text-blue-600 hover:bg-blue-50',
  };

  const sizeClasses = {
    sm: 'px-3 py-1.5 text-sm',
    md: 'px-4 py-2 text-base',
    lg: 'px-6 py-3 text-lg',
  };

  return (
    <button
      type={type}
      className={clsx(
        baseClasses,
        variantClasses[variant],
        sizeClasses[size],
        disabled && 'opacity-50 cursor-not-allowed',
        className
      )}
      onClick={onClick}
      disabled={disabled}
    >
      {children}
    </button>
  );
}
```

#### 2.2 สร้าง Input Component

**ไฟล์ `src/components/common/Input.tsx`:**

```typescript
import React from 'react';
import clsx from 'clsx';

interface InputProps {
  type?: 'text' | 'email' | 'password' | 'number';
  placeholder?: string;
  value?: string;
  onChange?: (e: React.ChangeEvent<HTMLInputElement>) => void;
  disabled?: boolean;
  error?: string;
  label?: string;
  required?: boolean;
}

export function Input({
  type = 'text',
  placeholder,
  value,
  onChange,
  disabled = false,
  error,
  label,
  required = false,
}: InputProps) {
  return (
    <div className="w-full">
      {label && (
        <label className="block text-sm font-medium text-gray-700 mb-1">
          {label} {required && <span className="text-red-600">*</span>}
        </label>
      )}
      <input
        type={type}
        placeholder={placeholder}
        value={value}
        onChange={onChange}
        disabled={disabled}
        className={clsx(
          'w-full px-4 py-2 border rounded-lg',
          'focus:outline-none focus:ring-2 focus:ring-blue-500',
          error ? 'border-red-500' : 'border-gray-300',
          disabled && 'bg-gray-100 cursor-not-allowed'
        )}
      />
      {error && <p className="mt-1 text-sm text-red-600">{error}</p>}
    </div>
  );
}
```

#### 2.3 สร้าง Card Component

**ไฟล์ `src/components/common/Card.tsx`:**

```typescript
import React from 'react';
import clsx from 'clsx';

interface CardProps {
  children: React.ReactNode;
  className?: string;
  onClick?: () => void;
  hover?: boolean;
}

export function Card({ children, className, onClick, hover = false }: CardProps) {
  return (
    <div
      className={clsx(
        'bg-white border border-gray-200 rounded-lg p-4',
        hover && 'transition-shadow hover:shadow-lg cursor-pointer',
        className
      )}
      onClick={onClick}
    >
      {children}
    </div>
  );
}
```

---

### 3. สร้าง Feature Components

#### 3.1 ProductCard Component

**ไฟล์ `src/components/features/products/ProductCard.tsx`:**

```typescript
import { Product } from '@/types/models';
import { Card } from '@/components/common/Card';
import { Button } from '@/components/common/Button';

interface ProductCardProps {
  product: Product;
}

export function ProductCard({ product }: ProductCardProps) {
  const handleAddToCart = () => {
    alert(`Added ${product.name} to cart!`);
  };

  return (
    <Card hover>
      <img
        src={product.imageUrl}
        alt={product.name}
        className="w-full h-48 object-cover rounded-md mb-3"
      />
      <h3 className="font-semibold text-lg mb-1 line-clamp-2">{product.name}</h3>
      <p className="text-sm text-gray-600 mb-2 line-clamp-2">{product.description}</p>

      <div className="flex items-center gap-2 mb-2">
        <span className="text-yellow-500">⭐</span>
        <span className="text-sm font-medium">{product.rating}</span>
        <span className="text-sm text-gray-500">({product.reviewCount} reviews)</span>
      </div>

      <div className="flex items-center justify-between mb-3">
        <div>
          <p className="text-xl font-bold text-blue-600">
            ฿{product.price.toLocaleString()}
          </p>
          {product.originalPrice && (
            <p className="text-sm text-gray-400 line-through">
              ฿{product.originalPrice.toLocaleString()}
            </p>
          )}
        </div>
        <span className="text-sm text-gray-600">Stock: {product.stock}</span>
      </div>

      <Button variant="primary" className="w-full" onClick={handleAddToCart}>
        Add to Cart
      </Button>
    </Card>
  );
}
```

#### 3.2 ProductList Component

**ไฟล์ `src/components/features/products/ProductList.tsx`:**

```typescript
import { Product } from '@/types/models';
import { ProductCard } from './ProductCard';

interface ProductListProps {
  products: Product[];
}

export function ProductList({ products }: ProductListProps) {
  if (products.length === 0) {
    return (
      <div className="text-center py-12">
        <p className="text-gray-500 text-lg">No products found</p>
      </div>
    );
  }

  return (
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
      {products.map((product) => (
        <ProductCard key={product.id} product={product} />
      ))}
    </div>
  );
}
```

---

### 4. สร้าง Layout Components

#### 4.1 Header Component

**ไฟล์ `src/components/layout/Header.tsx`:**

```typescript
import { Link } from 'react-router-dom';
import { FaShoppingCart, FaUser } from 'react-icons/fa';

export function Header() {
  return (
    <header className="bg-white border-b border-gray-200 sticky top-0 z-50">
      <div className="container mx-auto px-4">
        <div className="flex items-center justify-between h-16">
          {/* Logo */}
          <Link to="/" className="text-2xl font-bold text-blue-600">
            ShopLogo
          </Link>

          {/* Navigation */}
          <nav className="hidden md:flex items-center gap-6">
            <Link to="/" className="text-gray-700 hover:text-blue-600">
              Home
            </Link>
            <Link to="/products" className="text-gray-700 hover:text-blue-600">
              Products
            </Link>
            <Link to="/categories" className="text-gray-700 hover:text-blue-600">
              Categories
            </Link>
          </nav>

          {/* Actions */}
          <div className="flex items-center gap-4">
            <Link to="/cart" className="relative">
              <FaShoppingCart className="text-2xl text-gray-700 hover:text-blue-600" />
              <span className="absolute -top-2 -right-2 bg-red-600 text-white text-xs rounded-full w-5 h-5 flex items-center justify-center">
                0
              </span>
            </Link>
            <Link to="/login">
              <FaUser className="text-2xl text-gray-700 hover:text-blue-600" />
            </Link>
          </div>
        </div>
      </div>
    </header>
  );
}
```

#### 4.2 Footer Component

**ไฟล์ `src/components/layout/Footer.tsx`:**

```typescript
export function Footer() {
  return (
    <footer className="bg-gray-900 text-white mt-12">
      <div className="container mx-auto px-4 py-8">
        <div className="grid grid-cols-1 md:grid-cols-4 gap-8">
          <div>
            <h3 className="text-xl font-bold mb-4">ShopLogo</h3>
            <p className="text-gray-400">Your one-stop shop for everything</p>
          </div>

          <div>
            <h4 className="font-semibold mb-4">Quick Links</h4>
            <ul className="space-y-2 text-gray-400">
              <li><a href="/">Home</a></li>
              <li><a href="/products">Products</a></li>
              <li><a href="/about">About Us</a></li>
              <li><a href="/contact">Contact</a></li>
            </ul>
          </div>

          <div>
            <h4 className="font-semibold mb-4">Customer Service</h4>
            <ul className="space-y-2 text-gray-400">
              <li><a href="/help">Help Center</a></li>
              <li><a href="/returns">Returns</a></li>
              <li><a href="/shipping">Shipping Info</a></li>
              <li><a href="/terms">Terms & Conditions</a></li>
            </ul>
          </div>

          <div>
            <h4 className="font-semibold mb-4">Contact Us</h4>
            <p className="text-gray-400">Email: support@shop.com</p>
            <p className="text-gray-400">Phone: 02-xxx-xxxx</p>
          </div>
        </div>

        <div className="border-t border-gray-800 mt-8 pt-8 text-center text-gray-400">
          <p>&copy; 2025 ShopLogo. All rights reserved.</p>
        </div>
      </div>
    </footer>
  );
}
```

---

### 5. สร้าง Pages

#### 5.1 HomePage

**ไฟล์ `src/pages/HomePage.tsx`:**

```typescript
import { Header } from '@/components/layout/Header';
import { Footer } from '@/components/layout/Footer';
import { ProductList } from '@/components/features/products/ProductList';
import { mockProducts } from '@/data/mock/products';
import { Button } from '@/components/common/Button';
import { Link } from 'react-router-dom';

export function HomePage() {
  const featuredProducts = mockProducts.slice(0, 4);

  return (
    <div className="min-h-screen flex flex-col">
      <Header />

      <main className="flex-1">
        {/* Hero Section */}
        <section className="bg-gradient-to-r from-blue-600 to-blue-800 text-white py-20">
          <div className="container mx-auto px-4 text-center">
            <h1 className="text-5xl font-bold mb-4">Welcome to ShopLogo</h1>
            <p className="text-xl mb-8">Find everything you need in one place</p>
            <Link to="/products">
              <Button variant="secondary" size="lg">
                Shop Now
              </Button>
            </Link>
          </div>
        </section>

        {/* Featured Products */}
        <section className="container mx-auto px-4 py-12">
          <h2 className="text-3xl font-bold mb-8">Featured Products</h2>
          <ProductList products={featuredProducts} />

          <div className="text-center mt-8">
            <Link to="/products">
              <Button variant="outline" size="lg">
                View All Products
              </Button>
            </Link>
          </div>
        </section>
      </main>

      <Footer />
    </div>
  );
}
```

#### 5.2 ProductsPage

**ไฟล์ `src/pages/ProductsPage.tsx`:**

```typescript
import { useState } from 'react';
import { Header } from '@/components/layout/Header';
import { Footer } from '@/components/layout/Footer';
import { ProductList } from '@/components/features/products/ProductList';
import { mockProducts } from '@/data/mock/products';
import { mockCategories } from '@/data/mock/categories';
import { Input } from '@/components/common/Input';

export function ProductsPage() {
  const [searchQuery, setSearchQuery] = useState('');
  const [selectedCategory, setSelectedCategory] = useState<number | null>(null);

  const filteredProducts = mockProducts.filter((product) => {
    const matchesSearch = product.name.toLowerCase().includes(searchQuery.toLowerCase());
    const matchesCategory = selectedCategory === null || product.categoryId === selectedCategory;
    return matchesSearch && matchesCategory;
  });

  return (
    <div className="min-h-screen flex flex-col">
      <Header />

      <main className="flex-1 container mx-auto px-4 py-8">
        <h1 className="text-3xl font-bold mb-6">All Products</h1>

        {/* Search & Filter */}
        <div className="mb-8 flex flex-col md:flex-row gap-4">
          <div className="flex-1">
            <Input
              type="text"
              placeholder="Search products..."
              value={searchQuery}
              onChange={(e) => setSearchQuery(e.target.value)}
            />
          </div>

          <select
            className="px-4 py-2 border border-gray-300 rounded-lg"
            value={selectedCategory || ''}
            onChange={(e) => setSelectedCategory(e.target.value ? Number(e.target.value) : null)}
          >
            <option value="">All Categories</option>
            {mockCategories.map((cat) => (
              <option key={cat.id} value={cat.id}>
                {cat.name}
              </option>
            ))}
          </select>
        </div>

        {/* Results */}
        <p className="text-gray-600 mb-4">
          Showing {filteredProducts.length} products
        </p>

        <ProductList products={filteredProducts} />
      </main>

      <Footer />
    </div>
  );
}
```

#### 5.3 LoginPage (Simple Form)

**ไฟล์ `src/pages/LoginPage.tsx`:**

```typescript
import { useState } from 'react';
import { Link } from 'react-router-dom';
import { Input } from '@/components/common/Input';
import { Button } from '@/components/common/Button';

export function LoginPage() {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    alert(`Login: ${email}`);
  };

  return (
    <div className="min-h-screen bg-gray-100 flex items-center justify-center px-4">
      <div className="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
        <h1 className="text-3xl font-bold text-center mb-8">Login</h1>

        <form onSubmit={handleSubmit} className="space-y-4">
          <Input
            type="email"
            label="Email"
            placeholder="your@email.com"
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            required
          />

          <Input
            type="password"
            label="Password"
            placeholder="••••••••"
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            required
          />

          <Button type="submit" variant="primary" className="w-full">
            Login
          </Button>
        </form>

        <p className="text-center mt-4 text-gray-600">
          Don't have an account?{' '}
          <Link to="/register" className="text-blue-600 hover:underline">
            Register
          </Link>
        </p>

        <p className="text-center mt-2">
          <Link to="/" className="text-blue-600 hover:underline">
            Back to Home
          </Link>
        </p>
      </div>
    </div>
  );
}
```

---

### 6. Setup Routing

**ไฟล์ `src/App.tsx`:**

```typescript
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import { HomePage } from './pages/HomePage';
import { ProductsPage } from './pages/ProductsPage';
import { LoginPage } from './pages/LoginPage';

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<HomePage />} />
        <Route path="/products" element={<ProductsPage />} />
        <Route path="/login" element={<LoginPage />} />
      </Routes>
    </BrowserRouter>
  );
}

export default App;
```

---

### 7. ทดสอบ Demo

```bash
cd frontend
npm run dev
```

เปิด browser ที่ `http://localhost:5173`:
- ✅ หน้า Home แสดง Hero section และ Featured products
- ✅ หน้า Products แสดงสินค้าทั้งหมด พร้อม Search และ Filter
- ✅ หน้า Login มี form

---

## Checklist

- [ ] สร้าง Mock Data (products, categories, users, orders)
- [ ] สร้าง Common Components (Button, Input, Card)
- [ ] สร้าง Feature Components (ProductCard, ProductList)
- [ ] สร้าง Layout Components (Header, Footer)
- [ ] สร้าง Pages (HomePage, ProductsPage, LoginPage)
- [ ] Setup React Router
- [ ] ทดสอบ Navigation ระหว่างหน้า
- [ ] ทดสอบ Search และ Filter ทำงานได้
- [ ] Commit code

---

## Deliverable

**Web Application Demo 1.0** ที่มี:
1. หน้า Home พร้อม Hero และ Featured Products
2. หน้า Products พร้อม Search และ Category Filter
3. หน้า Login
4. Navigation ระหว่างหน้าทำงานได้
5. ใช้ Mock Data แสดงผล

---

## Next Steps

1. **5.5 Database Design** — ออกแบบฐานข้อมูลจาก Mock Data
2. **5.6 Backend Project** — ตั้งค่า Backend
