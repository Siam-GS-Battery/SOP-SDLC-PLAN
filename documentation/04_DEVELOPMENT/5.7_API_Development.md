# 5.7 API Development — พัฒนา API

## วัตถุประสงค์

พัฒนา REST API endpoints ตามที่ Frontend ต้องการ รวมถึงระบบ Authentication, Validation และ Error Handling

> **💡 สำหรับมือใหม่:** API Development คือการสร้าง "ประตู" ให้ Frontend เข้ามาดึงข้อมูลหรือจัดการข้อมูลในฐานข้อมูล โดยมี 3 ส่วนหลัก:
> 1. **Authentication** — ตรวจสอบว่าใครเข้ามา (Login/Register)
> 2. **Authorization** — ตรวจสอบว่าทำอะไรได้บ้าง (Customer vs Admin)
> 3. **Data Validation** — ตรวจสอบว่าข้อมูลที่ส่งมาถูกต้องหรือไม่

---

## ขั้นตอนการทำ

### 1. สร้างระบบ Authentication (JWT)

> **📌 แนวคิด:** JWT (JSON Web Token) คือ "บัตรผ่าน" ที่ออกให้ user หลัง login สำเร็จ ทุกครั้งที่เรียก API user จะส่ง token นี้มาเพื่อพิสูจน์ตัวตน

#### 1.1 สร้าง Auth Utils

**ไฟล์ `src/utils/jwt.ts`:**

```typescript
import jwt from 'jsonwebtoken';

const JWT_SECRET = process.env.JWT_SECRET!;
const JWT_EXPIRES_IN = process.env.JWT_EXPIRES_IN || '7d';

export function generateToken(payload: object): string {
  return jwt.sign(payload, JWT_SECRET, { expiresIn: JWT_EXPIRES_IN });
}

export function verifyToken(token: string) {
  return jwt.verify(token, JWT_SECRET);
}
```

**ไฟล์ `src/utils/bcrypt.ts`:**

```typescript
import bcrypt from 'bcrypt';

const SALT_ROUNDS = 10;

export async function hashPassword(password: string): Promise<string> {
  return bcrypt.hash(password, SALT_ROUNDS);
}

export async function comparePassword(password: string, hash: string): Promise<boolean> {
  return bcrypt.compare(password, hash);
}
```

> **⚠️ ข้อผิดพลาดที่พบบ่อย:**
>
> **1. JWT_SECRET ไม่มีค่า → แก้:** เพิ่มใน `.env`
> ```bash
> JWT_SECRET=your-super-secret-key-minimum-32-characters
> JWT_EXPIRES_IN=7d
> ```
>
> **2. ลืม `await` ตอน hash password → แก้:** ต้องใช้ `await hashPassword(password)` เสมอ
> ```typescript
> ❌ const hash = hashPassword(password); // ผิด! ได้ Promise ไม่ใช่ string
> ✅ const hash = await hashPassword(password); // ถูก!
> ```
>
> **3. เปรียบเทียบ password ผิด → แก้:** ใช้ `comparePassword()` อย่าใช้ `===`
> ```typescript
> ❌ if (password === user.password_hash) // ผิด! เปรียบเทียบไม่ได้
> ✅ const isValid = await comparePassword(password, user.password_hash); // ถูก!
> ```

---

#### 1.2 สร้าง Auth Middleware

> **📌 Middleware คืออะไร?** คือ "จุดตรวจ" ที่อยู่ระหว่าง Request กับ Controller — ใช้ตรวจสอบ token ก่อนที่จะให้ผ่านไปทำงานจริง

**ไฟล์ `src/middlewares/authMiddleware.ts`:**

```typescript
import { Request, Response, NextFunction } from 'express';
import { verifyToken } from '../utils/jwt';

export interface AuthRequest extends Request {
  user?: {
    id: number;
    email: string;
    role: string;
  };
}

export function authenticate(req: AuthRequest, res: Response, next: NextFunction) {
  try {
    const token = req.headers.authorization?.replace('Bearer ', '');

    if (!token) {
      return res.status(401).json({ success: false, message: 'No token provided' });
    }

    const decoded = verifyToken(token) as any;
    req.user = decoded;
    next();
  } catch (error) {
    return res.status(401).json({ success: false, message: 'Invalid token' });
  }
}

export function authorize(...roles: string[]) {
  return (req: AuthRequest, res: Response, next: NextFunction) => {
    if (!req.user) {
      return res.status(401).json({ success: false, message: 'Unauthorized' });
    }

    if (!roles.includes(req.user.role)) {
      return res.status(403).json({ success: false, message: 'Forbidden' });
    }

    next();
  };
}
```

> **⚠️ ข้อผิดพลาดที่พบบ่อย:**
>
> **1. ลืมใส่ "Bearer " ใน header → แก้:** Format ต้องเป็น `Bearer <token>`
> ```bash
> ❌ Authorization: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
> ✅ Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
> ```
>
> **2. ส่ง token ผ่าน body แทน headers → แก้:** JWT ต้องอยู่ใน Authorization header
> ```typescript
> ❌ { "token": "..." } // ส่งใน body
> ✅ headers: { "Authorization": "Bearer ..." } // ส่งใน header
> ```

---

#### 1.3 สร้าง Auth Controller

**ไฟล์ `src/controllers/authController.ts`:**

```typescript
import { Request, Response, NextFunction } from 'express';
import { pool } from '../config/database';
import { hashPassword, comparePassword } from '../utils/bcrypt';
import { generateToken } from '../utils/jwt';

export class AuthController {
  async register(req: Request, res: Response, next: NextFunction) {
    try {
      const { email, password, firstName, lastName } = req.body;

      // Check if user exists
      const existingUser = await pool.query('SELECT * FROM users WHERE email = $1', [email]);
      if (existingUser.rows.length > 0) {
        return res.status(400).json({ success: false, message: 'Email already exists' });
      }

      // Hash password
      const passwordHash = await hashPassword(password);

      // Create user
      const result = await pool.query(
        `INSERT INTO users (email, password_hash, first_name, last_name, role)
         VALUES ($1, $2, $3, $4, 'customer') RETURNING id, email, first_name, last_name, role`,
        [email, passwordHash, firstName, lastName]
      );

      const user = result.rows[0];

      // Generate token
      const token = generateToken({ id: user.id, email: user.email, role: user.role });

      res.status(201).json({
        success: true,
        data: { user, token },
      });
    } catch (error) {
      next(error);
    }
  }

  async login(req: Request, res: Response, next: NextFunction) {
    try {
      const { email, password } = req.body;

      // Find user
      const result = await pool.query(
        'SELECT * FROM users WHERE email = $1',
        [email]
      );

      if (result.rows.length === 0) {
        return res.status(401).json({ success: false, message: 'Invalid credentials' });
      }

      const user = result.rows[0];

      // Check password
      const isPasswordValid = await comparePassword(password, user.password_hash);
      if (!isPasswordValid) {
        return res.status(401).json({ success: false, message: 'Invalid credentials' });
      }

      // Generate token
      const token = generateToken({ id: user.id, email: user.email, role: user.role });

      res.json({
        success: true,
        data: {
          user: {
            id: user.id,
            email: user.email,
            firstName: user.first_name,
            lastName: user.last_name,
            role: user.role,
          },
          token,
        },
      });
    } catch (error) {
      next(error);
    }
  }

  async me(req: Request, res: Response, next: NextFunction) {
    try {
      const userId = (req as any).user.id;

      const result = await pool.query(
        'SELECT id, email, first_name, last_name, role FROM users WHERE id = $1',
        [userId]
      );

      if (result.rows.length === 0) {
        return res.status(404).json({ success: false, message: 'User not found' });
      }

      res.json({ success: true, data: result.rows[0] });
    } catch (error) {
      next(error);
    }
  }
}

export const authController = new AuthController();
```

> **⚠️ ข้อผิดพลาดที่พบบ่อย:**
>
> **1. Database query ไม่ได้ผลลัพธ์ → แก้:** ตรวจสอบชื่อตารางและ column
> ```typescript
> ❌ SELECT * FROM user // ผิด! ชื่อตารางคือ "users" ไม่ใช่ "user"
> ✅ SELECT * FROM users WHERE email = $1
> ```
>
> **2. Return ก่อนสร้าง token → แก้:** อย่า return ก่อนสร้าง token
> ```typescript
> ❌ if (!isPasswordValid) { return; } // ผิด! ไม่ส่ง response
> ✅ if (!isPasswordValid) {
>      return res.status(401).json({ success: false, message: 'Invalid credentials' });
>    }
> ```
>
> **3. เก็บ password แบบ plain text → แก้:** ต้อง hash เสมอ
> ```typescript
> ❌ INSERT INTO users (password) VALUES ($1) // ผิด! เก็บ plain text
> ✅ const passwordHash = await hashPassword(password);
>    INSERT INTO users (password_hash) VALUES ($1)
> ```

---

### 2. สร้าง Validation Middleware (Zod)

> **📌 ทำไมต้อง Validate?** เพื่อตรวจสอบข้อมูลที่ user ส่งมา — ถ้าไม่ validate อาจได้ข้อมูลผิดรูปแบบหรือไม่ครบถ้วน

**ไฟล์ `src/validators/authValidator.ts`:**

```typescript
import { z } from 'zod';

export const registerSchema = z.object({
  email: z.string().email('Invalid email format'),
  password: z.string().min(8, 'Password must be at least 8 characters'),
  firstName: z.string().min(1, 'First name is required'),
  lastName: z.string().min(1, 'Last name is required'),
});

export const loginSchema = z.object({
  email: z.string().email('Invalid email format'),
  password: z.string().min(1, 'Password is required'),
});
```

**ไฟล์ `src/middlewares/validation.ts`:**

```typescript
import { Request, Response, NextFunction } from 'express';
import { z } from 'zod';

export function validateRequest(schema: z.ZodSchema) {
  return (req: Request, res: Response, next: NextFunction) => {
    try {
      schema.parse(req.body);
      next();
    } catch (error) {
      if (error instanceof z.ZodError) {
        return res.status(400).json({
          success: false,
          errors: error.errors,
        });
      }
      next(error);
    }
  };
}
```

> **⚠️ ข้อผิดพลาดที่พบบ่อย:**
>
> **1. ลืม install Zod → แก้:** ติดตั้งก่อนใช้งาน
> ```bash
> npm install zod
> ```
>
> **2. Validation ไม่ทำงาน → แก้:** ต้องใช้ middleware ในลำดับที่ถูกต้อง
> ```typescript
> ❌ router.post('/register', authController.register, validateRequest(registerSchema));
> ✅ router.post('/register', validateRequest(registerSchema), authController.register);
> // Validation ต้องมาก่อน Controller!
> ```

---

### 3. สร้าง Routes

> **📌 Routes คืออะไร?** คือการกำหนด "เส้นทาง" ว่า URL ไหนจะไปทำงานที่ Controller ไหน

**ไฟล์ `src/routes/authRoutes.ts`:**

```typescript
import { Router } from 'express';
import { authController } from '../controllers/authController';
import { validateRequest } from '../middlewares/validation';
import { registerSchema, loginSchema } from '../validators/authValidator';
import { authenticate } from '../middlewares/authMiddleware';

const router = Router();

router.post('/register', validateRequest(registerSchema), authController.register);
router.post('/login', validateRequest(loginSchema), authController.login);
router.get('/me', authenticate, authController.me);

export default router;
```

**อัพเดท `src/routes/index.ts`:**

```typescript
import { Router } from 'express';
import authRoutes from './authRoutes';
import productRoutes from './productRoutes';

const router = Router();

router.use('/auth', authRoutes);
router.use('/products', productRoutes);

export default router;
```

> **⚠️ ข้อผิดพลาดที่พบบ่อย:**
>
> **1. ลืม register routes ใน main app → แก้:** ต้อง import ใน `src/routes/index.ts` หรือ `server.ts`
> ```typescript
> ❌ // ลืม import authRoutes
> ✅ import authRoutes from './authRoutes';
>    router.use('/auth', authRoutes);
> ```
>
> **2. URL path ซ้ำกัน → แก้:** ตรวจสอบ prefix ที่ซ้อนกัน
> ```typescript
> ❌ router.use('/api/auth', authRoutes); // ใน server.ts
>    router.use('/api/auth', ...); // ใน index.ts → ได้ /api/api/auth
> ✅ router.use('/api', routes); // ใน server.ts
>    router.use('/auth', authRoutes); // ใน index.ts → ได้ /api/auth
> ```

---

### 4. สร้าง Cart API

> **📌 หมายเหตุ:** Cart API ต้องมีการ authenticate ทุก endpoint เพราะต้องรู้ว่าเป็น cart ของ user คนไหน

**ไฟล์ `src/controllers/cartController.ts`:**

```typescript
import { Request, Response, NextFunction } from 'express';
import { pool } from '../config/database';
import { AuthRequest } from '../middlewares/authMiddleware';

export class CartController {
  async getCart(req: AuthRequest, res: Response, next: NextFunction) {
    try {
      const userId = req.user!.id;

      const result = await pool.query(
        `SELECT c.*, p.name, p.price, p.image_url, (c.quantity * p.price) as subtotal
         FROM carts c
         JOIN products p ON c.product_id = p.id
         WHERE c.user_id = $1`,
        [userId]
      );

      const items = result.rows;
      const total = items.reduce((sum, item) => sum + parseFloat(item.subtotal), 0);

      res.json({ success: true, data: { items, total } });
    } catch (error) {
      next(error);
    }
  }

  async addItem(req: AuthRequest, res: Response, next: NextFunction) {
    try {
      const userId = req.user!.id;
      const { productId, quantity } = req.body;

      // Check if item exists
      const existing = await pool.query(
        'SELECT * FROM carts WHERE user_id = $1 AND product_id = $2',
        [userId, productId]
      );

      if (existing.rows.length > 0) {
        // Update quantity
        await pool.query(
          'UPDATE carts SET quantity = quantity + $1 WHERE user_id = $2 AND product_id = $3',
          [quantity, userId, productId]
        );
      } else {
        // Insert new item
        await pool.query(
          'INSERT INTO carts (user_id, product_id, quantity) VALUES ($1, $2, $3)',
          [userId, productId, quantity]
        );
      }

      res.json({ success: true, message: 'Item added to cart' });
    } catch (error) {
      next(error);
    }
  }

  async updateItem(req: AuthRequest, res: Response, next: NextFunction) {
    try {
      const userId = req.user!.id;
      const { id } = req.params;
      const { quantity } = req.body;

      await pool.query(
        'UPDATE carts SET quantity = $1 WHERE id = $2 AND user_id = $3',
        [quantity, id, userId]
      );

      res.json({ success: true, message: 'Cart updated' });
    } catch (error) {
      next(error);
    }
  }

  async removeItem(req: AuthRequest, res: Response, next: NextFunction) {
    try {
      const userId = req.user!.id;
      const { id } = req.params;

      await pool.query('DELETE FROM carts WHERE id = $1 AND user_id = $2', [id, userId]);

      res.json({ success: true, message: 'Item removed from cart' });
    } catch (error) {
      next(error);
    }
  }
}

export const cartController = new CartController();
```

**ไฟล์ `src/routes/cartRoutes.ts`:**

```typescript
import { Router } from 'express';
import { cartController } from '../controllers/cartController';
import { authenticate } from '../middlewares/authMiddleware';

const router = Router();

router.use(authenticate); // All cart routes require authentication

router.get('/', cartController.getCart);
router.post('/add', cartController.addItem);
router.put('/:id', cartController.updateItem);
router.delete('/:id', cartController.removeItem);

export default router;
```

> **⚠️ ข้อผิดพลาดที่พบบ่อย:**
>
> **1. req.user เป็น undefined → แก้:** ต้องใช้ authenticate middleware ก่อน
> ```typescript
> ❌ router.get('/', cartController.getCart); // ไม่มี authenticate
> ✅ router.use(authenticate); // ใส่ middleware ทุก route
>    router.get('/', cartController.getCart);
> ```
>
> **2. JOIN ไม่ได้ข้อมูล → แก้:** ตรวจสอบ foreign key ว่าถูกต้อง
> ```sql
> ❌ JOIN products p ON c.product = p.id -- ผิด! column ชื่อ product_id
> ✅ JOIN products p ON c.product_id = p.id
> ```
>
> **3. การคำนวณ total ผิด → แก้:** ต้อง parseFloat ก่อนรวม
> ```typescript
> ❌ const total = items.reduce((sum, item) => sum + item.subtotal, 0); // ได้ string
> ✅ const total = items.reduce((sum, item) => sum + parseFloat(item.subtotal), 0);
> ```

---

### 5. ทดสอบ API ด้วย Postman/Insomnia

> **📌 เครื่องมือทดสอบ API:**
> - **Postman** — มี GUI สวยงาม เหมาะสำหรับมือใหม่
> - **Insomnia** — เบากว่า Postman
> - **curl** — ใช้ใน Terminal (สำหรับคนชอบ CLI)
> - **Thunder Client** — Extension ใน VSCode

#### 5.1 สร้าง Collection

**API Endpoints:**

```
POST   /api/auth/register     # สมัครสมาชิก
POST   /api/auth/login        # Login
GET    /api/auth/me           # ดูข้อมูล user (requires auth)

GET    /api/products          # รายการสินค้าทั้งหมด
GET    /api/products/:id      # รายละเอียดสินค้า
POST   /api/products          # สร้างสินค้าใหม่ (admin)
PUT    /api/products/:id      # แก้ไขสินค้า (admin)
DELETE /api/products/:id      # ลบสินค้า (admin)

GET    /api/cart              # ดูตะกร้า (requires auth)
POST   /api/cart/add          # เพิ่มสินค้า (requires auth)
PUT    /api/cart/:id          # อัพเดทจำนวน (requires auth)
DELETE /api/cart/:id          # ลบสินค้า (requires auth)
```

#### 5.2 Test Authentication Flow

```bash
# 1. Register
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"password123","firstName":"Test","lastName":"User"}'

# Response: { "success": true, "data": { "user": {...}, "token": "..." } }

# 2. Login
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"password123"}'

# 3. Get User (with token)
curl -X GET http://localhost:3000/api/auth/me \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

> **⚠️ ข้อผิดพลาดที่พบบ่อย:**
>
> **1. CORS Error เมื่อเรียกจาก Frontend → แก้:** เพิ่ม CORS middleware
> ```typescript
> import cors from 'cors';
> app.use(cors({
>   origin: 'http://localhost:5173', // Vite default port
>   credentials: true
> }));
> ```
>
> **2. ไม่สามารถ parse JSON body → แก้:** เพิ่ม middleware
> ```typescript
> app.use(express.json()); // ต้องมีบรรทัดนี้!
> ```
>
> **3. Database connection error → แก้:** ตรวจสอบ `.env`
> ```bash
> # ตรวจสอบว่า DATABASE_URL ถูกต้อง
> DATABASE_URL=postgresql://user:password@host:5432/dbname
> ```

---

## 6. Troubleshooting — แก้ปัญหาที่พบบ่อย

### 6.1 Token Issues

| ปัญหา | สาเหตุ | วิธีแก้ |
|-------|--------|---------|
| "Invalid token" | Token หมดอายุ | Login ใหม่เพื่อรับ token ใหม่ |
| "No token provided" | ลืมใส่ Authorization header | เพิ่ม `Authorization: Bearer <token>` |
| "JWT malformed" | Format token ผิด | ตรวจสอบว่าไม่มี whitespace หรือ newline ใน token |

### 6.2 Database Issues

| ปัญหา | สาเหตุ | วิธีแก้ |
|-------|--------|---------|
| "relation does not exist" | ชื่อตารางผิด | ตรวจสอบชื่อตารางใน SQL query |
| "column does not exist" | ชื่อ column ผิด | ตรวจสอบว่าใช้ snake_case (DB) ไม่ใช่ camelCase |
| "duplicate key value" | ข้อมูลซ้ำ (unique constraint) | ตรวจสอบว่า email หรือ key อื่นๆ ไม่ซ้ำ |

### 6.3 Password Issues

| ปัญหา | สาเหตุ | วิธีแก้ |
|-------|--------|---------|
| Password ไม่ match | ใช้ `===` แทน `comparePassword()` | ใช้ `await comparePassword(password, hash)` |
| "data and hash arguments required" | ส่งค่า undefined ให้ bcrypt | ตรวจสอบว่า `user.password_hash` มีค่า |
| Hash ไม่สำเร็จ | ลืม `await` | ใช้ `await hashPassword(password)` |

### 6.4 Testing Tips

**✅ ขั้นตอนการทดสอบที่ถูกต้อง:**

1. **Test Register ก่อน**
   ```bash
   POST /api/auth/register
   # บันทึก token ที่ได้รับ
   ```

2. **Test Login**
   ```bash
   POST /api/auth/login
   # บันทึก token ที่ได้รับ (ถ้าต่างจาก register)
   ```

3. **Test Protected Routes**
   ```bash
   GET /api/auth/me
   # ใส่ token ใน Authorization header
   ```

4. **Test ทุก Endpoint** ตามลำดับ:
   - GET → POST → PUT → DELETE

**🔍 Debug Tips:**

```typescript
// เพิ่ม console.log ใน middleware เพื่อ debug
export function authenticate(req: AuthRequest, res: Response, next: NextFunction) {
  console.log('Headers:', req.headers); // ดู headers ทั้งหมด
  console.log('Token:', token); // ดู token ที่ได้
  console.log('Decoded:', decoded); // ดูข้อมูลที่ decode ได้
}
```

---

## Deliverable

**API Collection (Postman/Insomnia)** ที่มี:
1. Auth endpoints (Register, Login, Me)
2. Product CRUD endpoints
3. Cart endpoints
4. JWT Authentication
5. Input validation (Zod)
6. Error handling

**Export Postman Collection:**
```json
{
  "info": { "name": "E-Commerce API" },
  "item": [
    { "name": "Auth", "item": [...] },
    { "name": "Products", "item": [...] },
    { "name": "Cart", "item": [...] }
  ]
}
```

---

## Next Steps

**5.8 Frontend Integration** — เชื่อมต่อ Frontend กับ API จริง
